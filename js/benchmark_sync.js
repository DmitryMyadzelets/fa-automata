// Generated by CoffeeScript 1.6.2
(function() {
  var cnt, foo, i, start, stop, sync, sys, table, _i;

  sync = function(m1, m2) {
    var common;

    common = [];
    return DES.sync(m1, m2, common);
  };

  sys = DES.modules[ix];

  cnt = 2;

  console.log('Parallel composition');

  start = window.performance.now();

  stop = start;

  table = [];

  foo = function() {
    var dt, m, ms, s;

    start = stop;
    sys = sync(DES.modules[ix], sys);
    stop = window.performance.now();
    dt = stop - start;
    s = (dt / 1000) | 0;
    m = (s / 60) | 0;
    s -= m * 60;
    ms = (dt - ((m * 60) + s) * 1000) | 0;
    console.log(cnt++, 'X:', sys.X.size(), 'T:', sys.T.size(), 'm:', m, 's:', s, 'ms:', ms);
    table.push({
      X: sys.X.size(),
      T: sys.T.size(),
      'm:s.ms': m + ':' + s + '.' + ms
    });
    if (m > 0) {
      console.log('Interruped due to the time limit');
      return true;
    }
    return null;
  };

  for (i = _i = 0; _i <= 3; i = ++_i) {
    if (foo()) {
      break;
    }
  }

  console.table(table);

}).call(this);
